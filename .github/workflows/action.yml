name: Buld and deploy workflow
on: [push]
jobs:
  build and push:
    runs-on: ubuntu-latest
    steps:
      - name: list dir
        run: ls -la
      - name: clone code
        uses: actions/checkout@v2
      - name: list dir 2
        run: ls -la